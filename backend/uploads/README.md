# Image Upload System

An empty placeholder file. The `uploads/` directory will contain user-uploaded images.

**Security Note:** This directory is protected by `.htaccess` to prevent execution of PHP scripts.
